@model IEnumerable<BankManagwmwntSystemEFWeb.Models.Entities.Overdraft>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AmountLeft)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.OverdraftDate)
            </th>



            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.AmountLeft)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.OverdraftDate)
                </td>



                <td>

                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |

                </td>
            </tr>
        }
    </tbody>
</table>
